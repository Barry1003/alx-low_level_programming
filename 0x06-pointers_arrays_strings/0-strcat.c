#include "main.h"
/**
 * _strcat -. is a function
 * @dest: first parameter
 * @src: second parameter
 * return out put
 */
 char *_strcat(char *dest, char *src);
{
    int i;
    int j;

    i = 0;
	j = 0

	while (dest[i] != '\0')
       	{
		i++;
	}

	while (ssrc[j] != '\0')
       	{
		j++;
		i++;
	}
	des[j] = '\0';

	return (dest);
}
